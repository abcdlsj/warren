{
  "name": "warren-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "node --test src/*.test.js && vite build"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.10.0",
    "@xterm/xterm": "5.5.0"
  },
  "devDependencies": {
    "vite": "7.1.1"
  },
  "allowScripts": {
    "esbuild": true,
    "fsevents": true
  }
}
